# 1.2.0

## General

- Added `PublishOnPlayback` behaviour to publish events from Animator states
- Added filter `OnlySelf` to use in `WithFilter` during event emission
- Added `Change log` in the documentation (this file)
- Added missing `Publish` methods in `GameEventSO`